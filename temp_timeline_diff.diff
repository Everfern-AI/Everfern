diff --git a/src/components/AgentTimeline.tsx b/src/components/AgentTimeline.tsx
index 1d61a99..1e7c839 100644
--- a/src/components/AgentTimeline.tsx
+++ b/src/components/AgentTimeline.tsx
@@ -21,22 +21,26 @@ export interface ToolCallDisplay {
     description?: string;
     phase?: "triage" | "planning" | "execution" | "validation" | "completion";
     thought?: string; // Individual thought for this step
+    currentNode?: string;
 }
 
 interface AgentTimelineProps {
     toolCalls: ToolCallDisplay[];
     thought?: string;
+    content?: string;
     isLive?: boolean;
     showOutput?: boolean;
     currentPhase?: "triage" | "planning" | "execution" | "validation" | "completion";
     currentNode?: string;
-    planSteps?: Array<{ id: string; description: string; tool?: string }> | null;
+    planSteps?: Array<{ id: string; title?: string; description: string; tool?: string }> | null;
     planTitle?: string | null;
     generatedTitle?: string;
     subAgentProgress?: Map<string, SubAgentProgressEvent[]>;
     timelineBranches?: Map<string, TimelineBranch>;
     debateData?: any;
     isDebating?: boolean;
+    missionTimeline?: any;
+    onPillClick?: (toolCall: ToolCallDisplay) => void;
 }
 
 // Import SubAgentProgressEvent type
@@ -1746,6 +1750,20 @@ const ToolRow = ({
     const hasSearchPills = isSearchTool && (queries.length > 0 || docs.length > 0);
     const hasExpandableContent = hasOutput || hasSearchResults || hasSearchPills;
 
+    // Debug logging for search tools
+    if (isSearchTool) {
+        console.log('[ToolRow] Search tool detected:', {
+            toolName: tc.toolName,
+            hasArgs: !!tc.args,
+            args: tc.args,
+            queries,
+            docs,
+            hasSearchPills,
+            hasOutput,
+            hasSearchResults
+        });
+    }
+
     // Auto-expand search tools when they have results (only once, not repeatedly)
     const [hasAutoExpanded, setHasAutoExpanded] = useState(false);
 
@@ -1849,7 +1867,7 @@ const ToolRow = ({
                     )}
                 </div>
 
-                {tc.description && !isExpanded && (
+                {tc.description && (
                     <div style={{
                         fontSize: 11.5, color: "#8a8886", marginTop: 2,
                         lineHeight: 1.5, fontFamily: "'Figtree', system-ui, sans-serif",
@@ -1858,6 +1876,100 @@ const ToolRow = ({
                     </div>
                 )}
 
+                {/* Inline Pills - Always Visible */}
+                {(hasSearchPills || docs.length > 0) && (
+                    <div style={{ marginTop: 8, display: 'flex', flexWrap: 'wrap', gap: 6 }}>
+                        {/* Query Pills */}
+                        {queries.map((q, i) => (
+                            <div key={`query-${i}`} style={{
+                                display: 'inline-flex',
+                                alignItems: 'center',
+                                gap: 4,
+                                padding: '4px 10px',
+                                borderRadius: 16,
+                                backgroundColor: '#f3f4f6',
+                                border: '1px solid transparent',
+                                fontSize: 11,
+                                color: '#374151',
+                                fontWeight: 500,
+                                fontFamily: "'Figtree', system-ui, sans-serif",
+                                cursor: 'pointer',
+                                transition: 'all 0.2s',
+                            }}
+                            onMouseEnter={(e) => {
+                                e.currentTarget.style.backgroundColor = '#e5e7eb';
+                                e.currentTarget.style.borderColor = '#d1d5db';
+                            }}
+                            onMouseLeave={(e) => {
+                                e.currentTarget.style.backgroundColor = '#f3f4f6';
+                                e.currentTarget.style.borderColor = 'transparent';
+                            }}
+                            onClick={() => hasExpandableContent && onToggle()}
+                            >
+                                <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="#6b7280" strokeWidth="2.5" strokeLinecap="round" strokeLinejoin="round">
+                                    <circle cx="11" cy="11" r="8" />
+                                    <path d="m21 21-4.35-4.35" />
+                                </svg>
+                                {q}
+                            </div>
+                        ))}
+
+                        {/* Doc Pills */}
+                        {docs.slice(0, 2).map((d, i) => (
+                            <div key={`doc-${i}`} style={{
+                                display: 'inline-flex',
+                                alignItems: 'center',
+                                gap: 4,
+                                padding: '4px 10px',
+                                borderRadius: 16,
+                                backgroundColor: '#f3f4f6',
+                                border: '1px solid transparent',
+                                fontSize: 11,
+                                color: '#374151',
+                                fontWeight: 500,
+                                fontFamily: "'Figtree', system-ui, sans-serif",
+                                cursor: 'pointer',
+                                transition: 'all 0.2s',
+                            }}
+                            onMouseEnter={(e) => {
+                                e.currentTarget.style.backgroundColor = '#e5e7eb';
+                                e.currentTarget.style.borderColor = '#d1d5db';
+                            }}
+                            onMouseLeave={(e) => {
+                                e.currentTarget.style.backgroundColor = '#f3f4f6';
+                                e.currentTarget.style.borderColor = 'transparent';
+                            }}
+                            onClick={() => hasExpandableContent && onToggle()}
+                            >
+                                <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="#3b82f6" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round">
+                                    <path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z" />
+                                    <polyline points="14,2 14,8 20,8" />
+                                    <line x1="16" y1="13" x2="8" y2="13" />
+                                    <line x1="16" y1="17" x2="8" y2="17" />
+                                    <polyline points="10,9 9,9 8,9" />
+                                </svg>
+                                {d}
+                            </div>
+                        ))}
+
+                        {docs.length > 2 && (
+                            <div style={{
+                                display: 'inline-flex',
+                                alignItems: 'center',
+                                fontSize: 11,
+                                color: '#2563eb',
+                                fontWeight: 600,
+                                cursor: 'pointer',
+                                fontFamily: "'Figtree', system-ui, sans-serif"
+                            }}
+                            onClick={() => hasExpandableContent && onToggle()}
+                            >
+                                + {docs.length - 2} more
+                            </div>
+                        )}
+                    </div>
+                )}
+
                 {/* Streaming artifact content preview */}
                 {isArtifact && isRunning && artifactContent && (
                     <motion.div
@@ -2275,435 +2387,601 @@ const SubAgentTimelineView = ({
     );
 };
 
-export const AgentTimeline = ({
-    toolCalls, thought, isLive, currentNode, planSteps, planTitle, subAgentProgress, timelineBranches, generatedTitle, debateData, isDebating
-}: AgentTimelineProps) => {
-    const [expandedToolId, setExpandedToolId] = useState<string | null>(null);
-    const [collapsed, setCollapsed] = useState(false);
-    const [collapsedBranches, setCollapsedBranches] = useState<Set<string>>(new Set());
-    const [activeSubAgentId, setActiveSubAgentId] = useState<string | null>(null);
-    const [tooltipState, setTooltipState] = useState<{
-        visible: boolean;
-        branch: TimelineBranch | null;
-        position: { x: number; y: number };
-    }>({ visible: false, branch: null, position: { x: 0, y: 0 } });
-
-    // Helper function to build timeline branches from subagent progress events
-    const buildTimelineBranches = useMemo((): Map<string, TimelineBranch> => {
-        const branches = new Map<string, TimelineBranch>();
-
-        if (timelineBranches) {
-            timelineBranches.forEach((branch, id) => {
-                branches.set(id, {
-                    ...branch,
-                    isCollapsed: collapsedBranches.has(id)
-                });
-            });
-            return branches;
+// ÔöÇÔöÇ Staged Task Block Custom Components (Manus Lite Redesign) ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ
+
+const CompletedIcon = () => (
+    <div style={{
+        display: "flex",
+        alignItems: "center",
+        justifyContent: "center",
+        width: 18,
+        height: 18,
+        borderRadius: "50%",
+        backgroundColor: "#10b981",
+        color: "#ffffff",
+        flexShrink: 0
+    }}>
+        <svg width="10" height="10" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="4" strokeLinecap="round" strokeLinejoin="round">
+            <polyline points="20 6 9 17 4 12" />
+        </svg>
+    </div>
+);
+
+const InProgressIcon = () => (
+    <motion.div
+        animate={{ rotate: 360 }}
+        transition={{ repeat: Infinity, duration: 1, ease: "linear" }}
+        style={{
+            width: 18,
+            height: 18,
+            borderRadius: "50%",
+            border: "2px solid rgba(59, 130, 246, 0.15)",
+            borderTopColor: "#3b82f6",
+            flexShrink: 0
+        }}
+    />
+);
+
+const PendingIcon = () => (
+    <div style={{
+        width: 18,
+        height: 18,
+        borderRadius: "50%",
+        border: "2px solid #e5e7eb",
+        backgroundColor: "transparent",
+        flexShrink: 0
+    }} />
+);
+
+const FailedIcon = () => (
+    <div style={{
+        display: "flex",
+        alignItems: "center",
+        justifyContent: "center",
+        width: 18,
+        height: 18,
+        borderRadius: "50%",
+        backgroundColor: "#ef4444",
+        color: "#ffffff",
+        flexShrink: 0
+    }}>
+        <svg width="10" height="10" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="4" strokeLinecap="round" strokeLinejoin="round">
+            <line x1="18" y1="6" x2="6" y2="18" />
+            <line x1="6" y1="6" x2="18" y2="18" />
+        </svg>
+    </div>
+);
+
+const ActionPill = ({ tc, onClick }: { tc: ToolCallDisplay; onClick?: () => void }) => {
+    const isRunning = tc.status === "running";
+    const isError = tc.status === "error";
+    
+    // Categorize tool call types
+    const toolLower = tc.toolName.toLowerCase();
+    let icon = 'ÔÜÖ´©Å';
+    let theme = {
+        text: '#4b5563',
+        bg: 'rgba(243, 244, 246, 0.65)',
+        border: 'rgba(229, 231, 235, 0.85)',
+        hoverBg: 'rgba(243, 244, 246, 0.85)'
+    };
+    
+    if (toolLower.includes('search') || toolLower.includes('query') || toolLower.includes('grep')) {
+        icon = '­ƒöì';
+        theme = {
+            text: '#2563eb',
+            bg: 'rgba(239, 246, 255, 0.7)',
+            border: 'rgba(191, 219, 254, 0.5)',
+            hoverBg: 'rgba(239, 246, 255, 0.85)'
+        };
+    } else if (toolLower.includes('browser') || toolLower.includes('navigate') || toolLower.includes('visit') || toolLower.includes('navis') || toolLower.includes('url')) {
+        icon = '­ƒîÉ';
+        theme = {
+            text: '#7c3aed',
+            bg: 'rgba(245, 243, 255, 0.7)',
+            border: 'rgba(233, 213, 255, 0.5)',
+            hoverBg: 'rgba(245, 243, 255, 0.85)'
+        };
+    } else if (toolLower.includes('file') || toolLower.includes('view') || toolLower.includes('read') || toolLower.includes('write') || toolLower.includes('replace') || toolLower.includes('dir') || toolLower.includes('list')) {
+        icon = '­ƒôØ';
+        theme = {
+            text: '#ea580c',
+            bg: 'rgba(255, 247, 237, 0.7)',
+            border: 'rgba(254, 215, 170, 0.5)',
+            hoverBg: 'rgba(255, 247, 237, 0.85)'
+        };
+    } else if (toolLower.includes('command') || toolLower.includes('run') || toolLower.includes('bash') || toolLower.includes('terminal') || toolLower.includes('execute')) {
+        icon = '­ƒÆ╗';
+        theme = {
+            text: '#059669',
+            bg: 'rgba(240, 253, 244, 0.7)',
+            border: 'rgba(187, 247, 208, 0.5)',
+            hoverBg: 'rgba(240, 253, 244, 0.85)'
+        };
+    }
+    
+    // Auto-extract a high-fidelity display name
+    let label = tc.label || tc.displayName || tc.toolName;
+    if (tc.toolName === 'web_search' || tc.toolName === 'remote_web_search') {
+        const query = tc.args?.query as string;
+        label = query ? `Search "${query.length > 24 ? query.slice(0, 21) + '...' : query}"` : 'Search';
+    } else if (tc.toolName === 'open_browser_url' || tc.toolName === 'browser_navigate') {
+        const url = (tc.args?.url as string) || (tc.args?.url_to_visit as string);
+        if (url) {
+            try {
+                const domain = new URL(url).hostname;
+                label = `Visit ${domain}`;
+            } catch {
+                label = `Visit ${url.length > 22 ? url.slice(0, 19) + '...' : url}`;
+            }
+        } else {
+            label = 'Visit Webpage';
         }
-
-        // Build flat branches from subagent progress events
-        if (subAgentProgress) {
-            subAgentProgress.forEach((events, toolCallId) => {
-                if (events.length === 0) return;
-
-                const branchGroups = new Map<string, SubAgentProgressEvent[]>();
-
-                events.forEach(event => {
-                    if (event.timelineBranch) {
-                        const branchId = event.timelineBranch.sessionId || `${toolCallId}-${event.timelineBranch.agentType}`;
-                        if (!branchGroups.has(branchId)) {
-                            branchGroups.set(branchId, []);
-                        }
-                        branchGroups.get(branchId)!.push(event);
-                    } else if ((event as any).swarmId) {
-                        const swarmId = (event as any).swarmId;
-                        if (!branchGroups.has(swarmId)) {
-                            branchGroups.set(swarmId, []);
-                        }
-                        branchGroups.get(swarmId)!.push(event);
-                    }
-                });
-
-                branchGroups.forEach((branchEvents, branchId) => {
-                    if (branchEvents.length === 0) return;
-
-                    const firstEvent = branchEvents[0];
-                    const lastEvent = branchEvents[branchEvents.length - 1];
-                    const branchMetadata = firstEvent.timelineBranch || (firstEvent as any).swarmMetadata;
-
-                    let status: TimelineBranch['status'] = 'running';
-                    if (lastEvent.type === 'complete' || (lastEvent as any).type === 'task_complete') status = 'completed';
-                    else if (lastEvent.type === 'abort') status = 'aborted';
-                    else if (branchMetadata?.branchStatus === 'failed') status = 'failed';
-
-                    const branch: TimelineBranch = {
-                        id: branchId,
-                        parentId: branchMetadata?.parentId || toolCallId,
-                        parentSessionKey: branchMetadata?.parentSessionKey,
-                        agentType: branchMetadata?.agentType || (firstEvent as any).agentType || 'research',
-                        events: branchEvents,
-                        status,
-                        startTime: firstEvent.timestamp,
-                        endTime: (lastEvent.type === 'complete' || lastEvent.type === 'abort' || (lastEvent as any).type === 'task_complete') ? lastEvent.timestamp : undefined,
-                        taskDescription: branchMetadata?.taskDescription || (firstEvent as any).task || 'Swarm Task',
-                        branchLevel: branchMetadata?.branchLevel || 1,
-                        isCollapsed: collapsedBranches.has(branchId)
-                    };
-
-                    branches.set(branchId, branch);
-                });
-            });
+    } else if (tc.toolName === 'write_to_file' || tc.toolName === 'replace_file_content' || tc.toolName === 'view_file' || tc.toolName === 'read_file' || tc.toolName === 'multi_replace_file_content') {
+        const file = (tc.args?.TargetFile as string) || (tc.args?.AbsolutePath as string);
+        if (file) {
+            const basename = file.split(/[\\/]/).pop();
+            const actionVerb = tc.toolName.includes('read') || tc.toolName.includes('view') ? 'Read' : 'Edit';
+            label = `${actionVerb} ${basename}`;
         }
+    } else if (tc.toolName === 'run_command') {
+        const cmd = tc.args?.CommandLine as string;
+        label = cmd ? `Run "${cmd.length > 22 ? cmd.slice(0, 19) + '...' : cmd}"` : 'Run Command';
+    }
+    
+    return (
+        <motion.button
+            whileHover={{ scale: 1.02 }}
+            whileTap={{ scale: 0.98 }}
+            onClick={onClick}
+            style={{
+                display: "inline-flex",
+                alignItems: "center",
+                gap: 6,
+                padding: "6px 14px",
+                borderRadius: 20,
+                backgroundColor: theme.bg,
+                border: `1px solid ${theme.border}`,
+                color: theme.text,
+                fontSize: 12,
+                fontWeight: 600,
+                cursor: "pointer",
+                transition: "all 0.15s ease",
+                boxShadow: "0 1px 2px rgba(0,0,0,0.02)",
+            }}
+        >
+            <span>{icon}</span>
+            <span>{label}</span>
+            
+            {/* Status indicator */}
+            {isRunning && (
+                <motion.div
+                    animate={{ scale: [1, 1.3, 1] }}
+                    transition={{ repeat: Infinity, duration: 1.2 }}
+                    style={{
+                        width: 6,
+                        height: 6,
+                        borderRadius: "50%",
+                        backgroundColor: theme.text,
+                        marginLeft: 4
+                    }}
+                />
+            )}
+            
+            {!isRunning && !isError && (
+                <svg width="10" height="10" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="4" strokeLinecap="round" strokeLinejoin="round" style={{ color: "#10b981", marginLeft: 4 }}>
+                    <polyline points="20 6 9 17 4 12" />
+                </svg>
+            )}
+            
+            {isError && (
+                <span style={{ color: "#ef4444", fontSize: 10, fontWeight: 800, marginLeft: 4 }}>ÔÜá´©Å</span>
+            )}
+        </motion.button>
+    );
+};
 
-        // Nest branches by branchLevel for hierarchical display
-        const sorted = [...branches.values()]
-            .sort((a, b) => a.startTime.localeCompare(b.startTime));
-
-        const nested = new Map<string, TimelineBranch>();
-        const lastAtLevel: Record<number, TimelineBranch> = {};
-
-        sorted.forEach(branch => {
-            if (branch.branchLevel <= 1) {
-                const top = { ...branch, childBranches: [] };
-                nested.set(branch.id, top);
-                lastAtLevel[1] = top;
-            } else {
-                const parentLevel = branch.branchLevel - 1;
-                const parent = lastAtLevel[parentLevel];
-                if (parent) {
-                    if (!parent.childBranches) parent.childBranches = [];
-                    parent.childBranches.push(branch);
-                    lastAtLevel[branch.branchLevel] = branch;
-                } else {
-                    const orphan = { ...branch, childBranches: [] };
-                    nested.set(branch.id, orphan);
-                }
-            }
-        });
-
-        return nested;
-    }, [subAgentProgress, timelineBranches, collapsedBranches]);
-
-    // Build unified timeline with thoughts interspersed in chronological order
-    const timelineItems = useMemo((): TimelineItem[] => {
-        const items: TimelineItem[] = [];
+const StagedTaskBlock = ({
+    step, onPillClick, isLive
+}: {
+    step: RenderingStep;
+    onPillClick?: (tc: ToolCallDisplay) => void;
+    isLive?: boolean;
+}) => {
+    const [isExpanded, setIsExpanded] = useState(step.status === 'in-progress' || step.status === 'completed');
+    
+    useEffect(() => {
+        if (step.status === 'in-progress') {
+            setIsExpanded(true);
+        }
+    }, [step.status]);
+    
+    const isCompleted = step.status === 'completed';
+    const isInProgress = step.status === 'in-progress';
+    const isFailed = step.status === 'failed';
+    const isPending = step.status === 'pending';
+    
+    let cardBg = "#ffffff";
+    let cardBorder = "#eae9e0";
+    let shadow = "0 1px 3px rgba(0,0,0,0.01)";
+    
+    if (isInProgress) {
+        cardBg = "#FAF9F5";
+        cardBorder = "rgba(99, 102, 241, 0.2)";
+        shadow = "0 4px 16px rgba(99, 102, 241, 0.03)";
+    } else if (isCompleted) {
+        cardBg = "#ffffff";
+        cardBorder = "#eae9e0";
+    }
+    
+    return (
+        <motion.div
+            layout="position"
+            transition={{ duration: 0.22 }}
+            style={{
+                backgroundColor: cardBg,
+                border: `1px solid ${cardBorder}`,
+                borderRadius: 14,
+                marginBottom: 14,
+                boxShadow: shadow,
+                overflow: "hidden",
+                transition: "background-color 0.25s ease, border-color 0.25s ease"
+            }}
+        >
+            {/* Header section */}
+            <div
+                onClick={() => setIsExpanded(!isExpanded)}
+                style={{
+                    display: "flex",
+                    alignItems: "center",
+                    justifyContent: "space-between",
+                    padding: "16px 20px",
+                    cursor: "pointer",
+                    userSelect: "none"
+                }}
+            >
+                <div style={{ display: "flex", alignItems: "center", gap: 12, flex: 1, minWidth: 0 }}>
+                    {isCompleted && <CompletedIcon />}
+                    {isInProgress && <InProgressIcon />}
+                    {isPending && <PendingIcon />}
+                    {isFailed && <FailedIcon />}
+                    
+                    <div style={{ display: "flex", flexDirection: "column", minWidth: 0 }}>
+                        <span style={{
+                            fontSize: 14,
+                            fontWeight: 650,
+                            color: isPending ? "#8a8886" : "#201e24",
+                            letterSpacing: "-0.015em",
+                            whiteSpace: "nowrap",
+                            overflow: "hidden",
+                            textOverflow: "ellipsis"
+                        }}>
+                            {step.title}
+                        </span>
+                        {step.description && !isExpanded && (
+                            <span style={{
+                                fontSize: 11.5,
+                                color: "#8a8886",
+                                whiteSpace: "nowrap",
+                                overflow: "hidden",
+                                textOverflow: "ellipsis",
+                                marginTop: 2
+                            }}>
+                                {step.description}
+                            </span>
+                        )}
+                    </div>
+                </div>
+                
+                <div style={{ display: "flex", alignItems: "center", gap: 8, paddingLeft: 12 }}>
+                    {step.toolCalls.length > 0 && !isExpanded && (
+                        <span style={{
+                            fontSize: 10.5,
+                            fontWeight: 700,
+                            backgroundColor: "#f5f4f0",
+                            color: "#8a8886",
+                            padding: "2px 8px",
+                            borderRadius: 10,
+                            border: "1px solid #eae9e0"
+                        }}>
+                            {step.toolCalls.length} step{step.toolCalls.length > 1 ? 's' : ''}
+                        </span>
+                    )}
+                    
+                    <motion.svg
+                        animate={{ rotate: isExpanded ? 0 : -90 }}
+                        transition={{ duration: 0.18 }}
+                        width="14"
+                        height="14"
+                        viewBox="0 0 24 24"
+                        fill="none"
+                        stroke="#b5b2aa"
+                        strokeWidth="2.5"
+                        strokeLinecap="round"
+                        strokeLinejoin="round"
+                    >
+                        <polyline points="6 9 12 15 18 9" />
+                    </motion.svg>
+                </div>
+            </div>
+            
+            {/* Collapsible Body */}
+            <AnimatePresence initial={false}>
+                {isExpanded && (
+                    <motion.div
+                        initial={{ height: 0, opacity: 0 }}
+                        animate={{ height: "auto", opacity: 1 }}
+                        exit={{ height: 0, opacity: 0 }}
+                        transition={{ duration: 0.2 }}
+                        style={{ overflow: "hidden" }}
+                    >
+                        <div style={{
+                            padding: "0px 20px 20px 20px",
+                            borderTop: "1px solid #fcfbf9",
+                            display: "flex",
+                            flexDirection: "column",
+                            gap: 12
+                        }}>
+                            {/* Step Description */}
+                            {step.description && (
+                                <span style={{
+                                    fontSize: 12.5,
+                                    color: "#8a8886",
+                                    lineHeight: "1.5",
+                                    marginBottom: 4
+                                }}>
+                                    {step.description}
+                                </span>
+                            )}
+                            
+                            {/* Thoughts paragraphs */}
+                            {step.thoughts.map((thought, idx) => (
+                                <div
+                                    key={`thought-${idx}`}
+                                    style={{
+                                        fontSize: 13,
+                                        color: "#8a8886",
+                                        lineHeight: "1.65",
+                                        backgroundColor: "#fcfbf9",
+                                        borderLeft: "2.5px solid #d1d0c5",
+                                        padding: "10px 14px",
+                                        borderRadius: "0 8px 8px 0",
+                                        fontStyle: "italic"
+                                    }}
+                                >
+                                    {thought}
+                                </div>
+                            ))}
+                            
+                            {/* Narrative Paragraphs */}
+                            {step.paragraphs.map((para, idx) => (
+                                <p
+                                    key={`para-${idx}`}
+                                    style={{
+                                        fontSize: 13,
+                                        color: "#4a4846",
+                                        lineHeight: "1.65",
+                                        margin: 0,
+                                        whiteSpace: "pre-wrap"
+                                    }}
+                                >
+                                    {para}
+                                </p>
+                            ))}
+                            
+                            {/* Action Pills Container */}
+                            {step.toolCalls.length > 0 && (
+                                <div style={{
+                                    display: "flex",
+                                    flexWrap: "wrap",
+                                    gap: 8,
+                                    marginTop: 4,
+                                    paddingTop: 12,
+                                    borderTop: "1px dashed #eae9e0"
+                                }}>
+                                    {step.toolCalls.map(tc => (
+                                        <ActionPill
+                                            key={tc.id}
+                                            tc={tc}
+                                            onClick={() => onPillClick?.(tc)}
+                                        />
+                                    ))}
+                                </div>
+                            )}
+                        </div>
+                    </motion.div>
+                )}
+            </AnimatePresence>
+        </motion.div>
+    );
+};
 
-        const hidden = ["write_file"];
-        const visibleTools = toolCalls.filter(tc => !hidden.includes(tc.toolName));
+interface RenderingStep {
+    id: string;
+    title: string;
+    description: string;
+    status: 'pending' | 'in-progress' | 'completed' | 'failed' | 'skipped';
+    toolCalls: ToolCallDisplay[];
+    paragraphs: string[];
+    thoughts: string[];
+}
 
-        if (thought && thought.trim()) {
-            items.push({
-                type: "thought",
-                data: { id: "global-thought", content: thought, isLive }
+export const AgentTimeline = ({
+    toolCalls, thought, content, isLive, currentNode, planSteps, planTitle, subAgentProgress, timelineBranches, generatedTitle, debateData, isDebating, missionTimeline, onPillClick
+}: AgentTimelineProps) => {
+    
+    // Dynamic grouping of timeline events and narrative text into sequential steps
+    const renderingSteps = useMemo((): RenderingStep[] => {
+        const steps: RenderingStep[] = [];
+        
+        const hiddenTools = ["write_file", "write_to_file", "write"];
+        const visibleToolCalls = toolCalls.filter(tc => !hiddenTools.includes(tc.toolName));
+
+        // 1. Initialize steps from decomposer plan or fallback
+        if (planSteps && planSteps.length > 0) {
+            planSteps.forEach(step => {
+                steps.push({
+                    id: step.id,
+                    title: step.title || step.description || "Execution Stage",
+                    description: step.description,
+                    status: 'pending',
+                    toolCalls: [],
+                    paragraphs: [],
+                    thoughts: []
+                });
+            });
+        } else if (missionTimeline?.steps && missionTimeline.steps.length > 0) {
+            missionTimeline.steps.forEach((mStep: any) => {
+                steps.push({
+                    id: mStep.id,
+                    title: mStep.name,
+                    description: mStep.description || "",
+                    status: mStep.status,
+                    toolCalls: [],
+                    paragraphs: [],
+                    thoughts: []
+                });
             });
         }
-
-        if (planSteps && planSteps.length > 0) {
-            items.push({
-                type: "plan",
-                data: { steps: planSteps, title: planTitle }
+        
+        // 1.5 Add a default step if we have toolCalls or thoughts but no structured plan steps
+        if (steps.length === 0 && (visibleToolCalls.length > 0 || (thought && thought.trim()))) {
+            steps.push({
+                id: 'default_stage',
+                title: generatedTitle || planTitle || 'Executing Task',
+                description: '',
+                status: 'in-progress',
+                toolCalls: [],
+                paragraphs: [],
+                thoughts: []
             });
         }
-
-        const toolNestedBranchIds = new Set<string>();
-
-        visibleTools.forEach(tc => {
-            if (tc.thought && tc.thought.trim()) {
-                items.push({
-                    type: "thought",
-                    data: { id: `thought-${tc.id}`, content: tc.thought, isLive: tc.status === "running" }
+        
+        // 2. Set statuses of each step based on live state
+        if (isLive) {
+            if (currentNode) {
+                let foundActive = false;
+                steps.forEach(step => {
+                    if (step.id === currentNode || step.title.toLowerCase().replace(/\s+/g, '_') === currentNode.toLowerCase()) {
+                        step.status = 'in-progress';
+                        foundActive = true;
+                    } else if (!foundActive) {
+                        step.status = 'completed';
+                    } else {
+                        step.status = 'pending';
+                    }
                 });
-            }
-
-            items.push({ type: "tool", data: tc });
-
-            buildTimelineBranches.forEach(branch => {
-                if (branch.parentId === tc.id) {
-                    items.push({ type: "timeline-branch", data: branch });
-                    toolNestedBranchIds.add(branch.id);
+                
+                // If currentNode is a system triage/debate node, mark first step active
+                if (!foundActive && steps[0]) {
+                    steps[0].status = 'in-progress';
                 }
+            } else if (steps[0]) {
+                steps[0].status = 'in-progress';
+            }
+        } else {
+            // Completed timeline has all steps completed
+            steps.forEach(step => {
+                step.status = 'completed';
             });
-
-            if (subAgentProgress && subAgentProgress.has(tc.id)) {
-                const progressEvents = subAgentProgress.get(tc.id) || [];
-                progressEvents.forEach(event => {
-                    if (!event.timelineBranch) {
-                        items.push({ type: "subagent-progress", data: event });
-                    }
-                });
+        }
+        
+        // 3. Map tool executions to steps
+        let currentStepIdx = 0;
+        visibleToolCalls.forEach(tc => {
+            let matchedStep = steps.find(s => s.id === tc.currentNode || s.id === (tc as any).associatedStepId);
+            
+            if (!matchedStep && missionTimeline?.steps) {
+                const mStep = missionTimeline.steps.find((s: any) => 
+                    s.toolCalls?.includes(tc.toolName) || s.toolCalls?.includes(tc.id)
+                );
+                if (mStep) {
+                    matchedStep = steps.find(s => s.id === mStep.id || s.title === mStep.name);
+                }
             }
-        });
-
-        // Add standalone branches (not nested under any tool) in chronological order
-        const standaloneBranches: TimelineBranch[] = [];
-        buildTimelineBranches.forEach(branch => {
-            if (!toolNestedBranchIds.has(branch.id)) {
-                standaloneBranches.push(branch);
+            
+            if (!matchedStep) {
+                // Match by keyword
+                matchedStep = steps.find((s, idx) => 
+                    idx >= currentStepIdx && 
+                    (s.description.toLowerCase().includes(tc.toolName.toLowerCase()) || 
+                     s.title.toLowerCase().includes(tc.toolName.toLowerCase()))
+                );
             }
-        });
-        standaloneBranches.sort((a, b) => a.startTime.localeCompare(b.startTime));
-        standaloneBranches.forEach(branch => {
-            items.push({ type: "timeline-branch", data: branch });
-        });
-
-        // Add debate ui element if we are debating or have debate data
-        // Only if it belongs globally, assuming debate happens at the main level
-        // We'll place it at the end so it's visible, or right after plan. Let's put it at the very top, like plan.
-        // Debate information is now attached to the prompt input area, not displayed in timeline
-
-        return items;
-    }, [toolCalls, thought, isLive, planSteps, planTitle, subAgentProgress, buildTimelineBranches, debateData, isDebating]);
-
-    const toggleTool = (id: string) => setExpandedToolId(p => p === id ? null : id);
-
-    const toggleBranchCollapse = (branchId: string) => {
-        setCollapsedBranches(prev => {
-            const newSet = new Set(prev);
-            if (newSet.has(branchId)) {
-                newSet.delete(branchId);
+            
+            if (!matchedStep) {
+                matchedStep = steps.find(s => s.status === 'in-progress');
+            }
+            
+            if (!matchedStep) {
+                matchedStep = steps[currentStepIdx] || steps[steps.length - 1];
             } else {
-                newSet.add(branchId);
+                currentStepIdx = steps.indexOf(matchedStep);
             }
-            return newSet;
-        });
-    };
-
-    const showTooltip = (event: React.MouseEvent, branch: TimelineBranch) => {
-        const rect = event.currentTarget.getBoundingClientRect();
-        setTooltipState({
-            visible: true,
-            branch,
-            position: {
-                x: rect.right + 10,
-                y: rect.top
+            
+            if (matchedStep) {
+                matchedStep.toolCalls.push(tc);
             }
         });
-    };
-
-    const hideTooltip = () => {
-        setTooltipState(prev => ({ ...prev, visible: false }));
-    };
-
-    const hasContent = timelineItems.length > 0;
-
-    // Don't show timeline if live but no content yet (hide "Initializing..." state)
-    if (!hasContent) return null;
+        
+        // 4. Distribute thought stream
+        if (thought && thought.trim()) {
+            const activeStep = steps.find(s => s.status === 'in-progress') || steps[0];
+            if (activeStep) {
+                activeStep.thoughts.push(thought.trim());
+            }
+        }
+        
+        // 5. Interleave narrative paragraphs chronologically
+        if (content && content.trim()) {
+            const paragraphs = content.split('\n\n').map(p => p.trim()).filter(Boolean);
+            let stepIdx = 0;
+            
+            paragraphs.forEach(para => {
+                if (stepIdx < steps.length - 1) {
+                    const nextStep = steps[stepIdx + 1];
+                    const nextTitle = nextStep.title.toLowerCase();
+                    const nextDesc = nextStep.description.toLowerCase();
+                    const paraLower = para.toLowerCase();
+                    
+                    if (paraLower.includes(nextTitle) || paraLower.includes(nextDesc.slice(0, 30))) {
+                        stepIdx++;
+                    }
+                }
+                
+                steps[stepIdx].paragraphs.push(para);
+            });
+        }
+        
+        return steps;
+    }, [toolCalls, thought, content, isLive, currentNode, planSteps, planTitle, generatedTitle, missionTimeline]);
 
-    // Count total steps (tools only, not thoughts/plans)
-    const totalSteps = timelineItems.filter(item => item.type === "tool").length;
+    // Check if we have any valid items to show
+    const hasTimelineItems = renderingSteps.length > 0;
 
-    // Generate header label
-    const headerLabel = isLive
-        ? currentNode
-            ? currentNode.replace(/_/g, " ")
-            : "Working"
-        : planTitle || generatedTitle || (totalSteps > 0
-            ? `Completed ┬À ${totalSteps} step${totalSteps !== 1 ? "s" : ""}`
-            : "Completed");
+    if (!hasTimelineItems) return null;
 
     return (
         <motion.div
             initial={{ opacity: 0, y: 4 }}
             animate={{ opacity: 1, y: 0 }}
-            transition={{ duration: 0.22 }}
+            transition={{ duration: 0.25 }}
             style={{
                 marginBottom: 16,
                 fontFamily: "'Figtree', system-ui, sans-serif",
                 paddingLeft: 0,
             }}
         >
-            {/* Timeline start indicator - clickable header */}
-            <button
-                onClick={() => setCollapsed(c => !c)}
-                style={{
-                    display: "flex",
-                    alignItems: "center",
-                    gap: 12,
-                    marginBottom: collapsed ? 0 : 12,
-                    paddingLeft: 0,
-                    background: "none",
-                    border: "none",
-                    cursor: "pointer",
-                    padding: 0,
-                    width: "100%",
-                }}
-            >
-                <div style={{
-                    position: "relative",
-                    display: "flex",
-                    alignItems: "center",
-                    justifyContent: "center",
-                    width: 20
-                }}>
-                    {/* Vertical line connecting to timeline */}
-                    {!collapsed && hasContent && (
-                        <div style={{
-                            position: "absolute",
-                            top: 10,
-                            width: 2,
-                            height: 18,
-                            backgroundColor: "#e8e6d9",
-                        }} />
-                    )}
-
-                    {isLive ? (
-                        <div style={{ position: "relative", width: 10, height: 10, flexShrink: 0 }}>
-                            <div style={{
-                                width: 10,
-                                height: 10,
-                                borderRadius: "50%",
-                                backgroundColor: "#6366f1",
-                                border: "2px solid #faf9f7",
-                                boxShadow: "0 0 0 1px #e8e6d9",
-                            }} />
-                        </div>
-                    ) : (
-                        <div style={{
-                            width: 10,
-                            height: 10,
-                            borderRadius: "50%",
-                            backgroundColor: "#22c55e",
-                            border: "2px solid #faf9f7",
-                            boxShadow: "0 0 0 1px #e8e6d9",
-                            flexShrink: 0
-                        }} />
-                    )}
-                </div>
-
-                <span style={{
-                    fontSize: 13,
-                    fontWeight: 600,
-                    color: "#201e24",
-                    letterSpacing: "-0.01em",
-                    textTransform: "capitalize",
-                    flex: 1,
-                    textAlign: "left",
-                }}>
-                    {headerLabel}
-                </span>
-
-                {/* Chevron indicator */}
-                <motion.svg
-                    animate={{ rotate: collapsed ? -90 : 0 }}
-                    transition={{ duration: 0.18 }}
-                    width={14} height={14} viewBox="0 0 24 24"
-                    fill="none" stroke="#b5b2aa" strokeWidth={2.5}
-                    strokeLinecap="round" strokeLinejoin="round"
-                    style={{ flexShrink: 0 }}
-                >
-                    <polyline points="6 9 12 15 18 9" />
-                </motion.svg>
-            </button>
-
-            {/* Timeline items - collapsible */}
-            <AnimatePresence initial={false}>
-                {!collapsed && (
-                    <motion.div
-                        key="timeline-content"
-                        initial={{ height: 0, opacity: 0 }}
-                        animate={{ height: "auto", opacity: 1 }}
-                        exit={{ height: 0, opacity: 0 }}
-                        transition={{ duration: 0.2, ease: "easeInOut" }}
-                        style={{ overflow: "hidden" }}
-                    >
-                        {activeSubAgentId && buildTimelineBranches.has(activeSubAgentId) ? (
-                            <SubAgentTimelineView
-                                branch={buildTimelineBranches.get(activeSubAgentId)!}
-                                onBack={() => setActiveSubAgentId(null)}
-                            />
-                        ) : (
-                            <div style={{ paddingLeft: 0 }}>
-                                {timelineItems.map((item, idx) => {
-                                    const isLast = idx === timelineItems.length - 1;
-
-                                    if (item.type === "thought") {
-                                        return (
-                                            <ThoughtItem
-                                                key={item.data.id}
-                                                content={item.data.content}
-                                                isLive={item.data.isLive}
-                                                isLast={isLast}
-                                            />
-                                        );
-                                    }
-
-                                    if (item.type === "plan") {
-                                        return (
-                                            <PlanItem
-                                                key="plan"
-                                                steps={item.data.steps}
-                                                title={item.data.title}
-                                                isLast={isLast}
-                                            />
-                                        );
-                                    }
-
-                                    if (item.type === "tool") {
-                                        return (
-                                            <ToolRow
-                                                key={item.data.id || idx}
-                                                tc={item.data}
-                                                isExpanded={expandedToolId === item.data.id}
-                                                onToggle={() => toggleTool(item.data.id)}
-                                                isLast={isLast}
-                                            />
-                                        );
-                                    }
-
-                                    if (item.type === "subagent-progress") {
-                                        return (
-                                            <div
-                                                key={`progress-${item.data.toolCallId}-${item.data.timestamp}-${idx}`}
-                                                style={{
-                                                    marginLeft: 20,
-                                                    backgroundColor: "#faf9f7",
-                                                    border: "1px solid #e8e6d9",
-                                                    borderRadius: 8,
-                                                    padding: 12,
-                                                    marginBottom: 12,
-                                                }}
-                                            >
-                                                <SubAgentProgressItem
-                                                    event={item.data}
-                                                    isLast={isLast}
-                                                />
-                                            </div>
-                                        );
-                                    }
-
-                                    if (item.type === "timeline-branch") {
-                                        return (
-                                            <TimelineBranchItem
-                                                key={item.data.id}
-                                                branch={item.data}
-                                                isLast={isLast}
-                                                onToggleCollapse={toggleBranchCollapse}
-                                                onShowTooltip={showTooltip}
-                                                onHideTooltip={hideTooltip}
-                                                onViewTimeline={(branchId) => setActiveSubAgentId(branchId)}
-                                            />
-                                        );
-                                    }
-
-                                    return null;
-                                })}
-                            </div>
-                        )}
-                    </motion.div>
-                )}
-            </AnimatePresence>
-
-            {/* Tooltip for branch details */}
-            <AnimatePresence>
-                {tooltipState.visible && (
-                    <BranchTooltip
-                        branch={tooltipState.branch}
-                        position={tooltipState.position}
-                        visible={tooltipState.visible}
+            <div style={{ display: "flex", flexDirection: "column", gap: 0 }}>
+                {renderingSteps.map((step) => (
+                    <StagedTaskBlock
+                        key={step.id}
+                        step={step}
+                        onPillClick={onPillClick}
+                        isLive={isLive}
                     />
-                )}
-            </AnimatePresence>
+                ))}
+            </div>
         </motion.div>
     );
 };
